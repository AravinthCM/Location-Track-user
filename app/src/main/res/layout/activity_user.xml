<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:orientation="vertical"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    tools:context=".UserActivity">

    <TextView
        android:id="@+id/textReceivedLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="User's Received Location"
        android:textSize="18sp"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/receivedLocationTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="User's Received Location"
        android:textSize="18sp"
        android:textStyle="bold"/>

    <fragment
        android:id="@+id/mapFragmentUser"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/textReceivedLocation"
        android:layout_marginTop="8dp"/>

    <!-- Add other UI elements as needed for UserActivity -->

</LinearLayout>
